import { useState, useReducer, useEffect } from 'react'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Grid } from '@material-ui/core'

import { Interlock } from './Interlock'

<Meta title="Interlock/Interlock" component={Interlock}/>

# Interlock

An interlock widget showing the status of an equipment. There are 3 statuses: normal, error and disconnected

## Basic

Read-only interlocks

<Preview>
  <Story name="Basic">
    {
       () => {
         const statuses = [ 'normal', 'error', 'disconnected' ]
         return (
           <Grid container spacing={2} direction={'row'}>
            {
              statuses.map(status => (
                <>
                  <Grid item>
                    <Interlock label={status} status={status}/>
                  </Grid>
                  <Grid item>
                    <Interlock label={status} status={status} ignored/>
                  </Grid>
                </>
              ))
            }
           </Grid>
         )
       }
    }
  </Story>
</Preview>

## API

<Props of={Interlock}/>
