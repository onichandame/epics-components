import { useEffect, useReducer } from 'react'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Button } from '@material-ui/core'

import Subject, {ButtonPanelRow} from './ButtonPanel'

<Meta title="Panel/Button Panel" component={Subject}/>

# Button Panel

A panel of row based buttons

## Basic

<Preview>
  <Story name="Basic">
    <div style={{width:' 500px'}}>
      <Subject>
        <ButtonPanelRow>
          <Button variant={'contained'}>
            Button1
          </Button>
          <Button variant={'contained'}>
            Button2
          </Button>
        </ButtonPanelRow>
        <ButtonPanelRow>
          <Button variant={'contained'}>
            Button3
          </Button>
          <Button variant={'contained'}>
            Button4
          </Button>
          <Button variant={'contained'}>
            Button5
          </Button>
        </ButtonPanelRow>
      </Subject>
    </div>
  </Story>
</Preview>

## API

<Props of={Subject}/>
